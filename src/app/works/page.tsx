import { WorksGallery } from "@/components/works/works-gallery";

export const metadata = {
  title: "Works — Studio Tahsin",
  description:
    "Selected projects with Problem → Approach → Impact case studies — PayLite X, StudentMove, applied ML, and more.",
};

export default function WorksPage() {
  return <WorksGallery />;
}
